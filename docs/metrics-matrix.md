# Detection quality matrix — per rule

Generated by `scripts/generate_metrics_matrix.py`. **Do not edit.** CI regenerates it and
fails if the committed file differs, so a rule added to `Detections/` cannot be missing here.

- **Rules:** 18 scheduled analytics rules
- **Validation status:** STATIC VALIDATION for all 18 — see
  [`../tests/atomics.md`](../tests/atomics.md)
- **Measurement columns:** every one reads `Not yet measured` because no rule has been
  deployed and no incident has been closed. The procedure that fills them, and the queries it
  uses, are in [`../tests/validation/performance-metrics.md`](../tests/validation/performance-metrics.md).

## Detection quality

| Rule | Severity | Telemetry | Expected volume (stated by the author, unmeasured) | How it would be exercised | Status |
|---|---|---|---|---|---|
| `Azure_KeyVault_AccessControlChange`<br>Azure - Key Vault Access Policy or Network ACL Modified | High | `AzureActivity` | Low. Higher during platform onboarding projects, which is exactly when the tuning log should record a spike. | Manual procedure | STATIC VALIDATION |
| `Azure_KeyVault_SecretAccessSpike`<br>Azure - Key Vault Secret Access Spike Per Identity | High | `AzureDiagnostics` | Very low; spikes are rare and high-signal. | Manual procedure | STATIC VALIDATION |
| `Azure_NSG_OpenToInternet`<br>Azure - NSG Rule Opens Port to 0.0.0.0/0 | High | `AzureActivity` | Low; every incident deserves review because of the direct exposure impact. | Manual procedure | STATIC VALIDATION |
| `EntraID_ImpossibleTravel`<br>Entra ID - Impossible Travel Between Sign-Ins | High | `SigninLogs` | Low. Typically a handful of incidents per week in a 1k-5k seat tenant after VPN tuning. | Manual procedure | STATIC VALIDATION |
| `EntraID_LegacyAuthSuccess`<br>Entra ID - Successful Legacy Auth Sign-In | High | `SigninLogs` | Near zero once legacy auth is blocked; spikes indicate CA gaps or password spray success. | Manual procedure | STATIC VALIDATION |
| `EntraID_MFAFatigue`<br>Entra ID - MFA Fatigue Followed by Success | High | `SigninLogs` | Very low in tenants with MFA number matching enforced. | Manual procedure | STATIC VALIDATION |
| `EntraID_PrivilegedRoleAssignment`<br>Entra ID - Privileged Role Granted to User or Service Principal | Medium | `AuditLogs` | Low. Expect zero to a handful per week in a tenant with a controlled privileged-access process. | Atomic Red Team: `T1098.003-1` (+1 partial) | STATIC VALIDATION |
| `EntraID_ServicePrincipalCredAdd`<br>Entra ID - Service Principal Credential Added | High | `AuditLogs` | Low; every event is worth reviewing given the TTP's track record. | Atomic Red Team: `T1098.001-1`, `T1098.001-2` | STATIC VALIDATION |
| `M365_InboxRuleExfil`<br>M365 - Suspicious Inbox Rule (Forward / Delete) | High | `OfficeActivity` | Very low; typically single incidents per week or less in a tenant that blocks external forwarding. | Atomic Red Team: `T1114.003-1`, `T1564.008-1` | STATIC VALIDATION |
| `M365_MassSharePointDownload`<br>M365 - Mass SharePoint / OneDrive Download | Medium | `OfficeActivity` | Low after service-account exclusion; z-score branch keeps most noise down. | Manual procedure | STATIC VALIDATION |
| `M365_OAuthConsentSuspiciousApp`<br>M365 - OAuth Consent to High-Risk Scopes | High | `AuditLogs` | Low in tenants with the admin-consent workflow enabled. | Manual procedure | STATIC VALIDATION |
| `MDE_LOLBin_Rundll32_Network`<br>MDE - rundll32 With External Network Connection | High | `DeviceNetworkEvents`, `DeviceProcessEvents` | Low; a few incidents per week in a 1k-seat estate, mostly proxy noise. | Atomic Red Team: `T1218.011-1` | STATIC VALIDATION |
| `MDE_MSHTA_RemoteScript`<br>MDE - mshta Executing Remote Script | High | `DeviceProcessEvents` | Very low; should be near zero in a hardened estate. | Atomic Red Team: `T1218.005-3`, `T1218.005-6` | STATIC VALIDATION |
| `MDE_PowerShell_EncodedCommand`<br>MDE - PowerShell EncodedCommand With Long Payload | High | `DeviceProcessEvents` | Low in a managed estate; bursts during software rollouts. | Atomic Red Team: `T1059.001-15`, `T1059.001-16` (+1 partial) | STATIC VALIDATION |
| `MDE_PsExec_ServiceExecution`<br>MDE - PsExec-Style Service Execution | Medium | `DeviceProcessEvents` | Low to medium. Depends directly on whether your estate runs scheduled PsExec automation. | Atomic Red Team: `T1569.002-1`, `T1569.002-2`, `T1569.002-3`, `T1021.002-3` | STATIC VALIDATION |
| `MDE_Ransomware_MassFileRename`<br>MDE - Ransomware-Style Mass File Rename Per Device | High | `DeviceFileEvents` | Very low. Expect zero outside migration windows on a typical estate. | Manual procedure (+1 partial atomic noted) | STATIC VALIDATION |
| `MDE_ShadowCopyDeletion`<br>MDE - Shadow Copy / Recovery Inhibited | High | `DeviceProcessEvents` | Very low. Near zero on a well-managed estate outside backup maintenance windows. | Atomic Red Team: `T1490-1`, `T1490-2`, `T1490-3`, `T1490-4`, `T1490-13` | STATIC VALIDATION |
| `MDE_WinRM_RemoteExecution`<br>MDE - WinRM Remote Command Execution | Medium | `DeviceProcessEvents` | Medium in estates with PowerShell-remoting automation; low otherwise. Expect bursts during patching windows. | Atomic Red Team: `T1021.006-2`, `T1021.006-3` | STATIC VALIDATION |

## Measurement framework

The columns below are the agreed field set. They are deliberately **empty of values**: each
one requires a deployed rule and closed incidents, and inventing a number would be worse than
leaving it blank. Definitions and the KQL that produces each column live in
[`../tests/validation/performance-metrics.md`](../tests/validation/performance-metrics.md).

| Field | Definition | Source |
|---|---|---|
| Events evaluated | Rows the rule's query scanned in the window | Workspace query cost / `Usage` table |
| Alerts | Alerts the rule produced | `SecurityAlert` |
| True positives | Closures classified a true positive | `SecurityIncident.Classification` |
| False positives | Closures classified a false positive | `SecurityIncident.Classification` |
| Precision | TP ÷ (TP + FP), on ≥ 5 closures | Derived from the two rows above |
| FP rate | FP ÷ (TP + FP), on ≥ 5 closures | Derived |
| Alert volume / day | Alerts per calendar day | `SecurityAlert` grouped by `TimeGenerated` |
| Latency p95 | Event time → alert time, 95th percentile | `FirstActivityTime` → `TimeGenerated` |
| MTTA | Incident created → first owner assignment | `CreatedTime` → `FirstModifiedTime` |
| MTTR | Incident created → closed | `CreatedTime` → `ClosedTime` |
| Suppression rate | Alerts suppressed by tuning rather than by detection | `metadata.suppression` decisions per rule |
| Closure reason | Why an incident was closed | `SecurityIncident.Classification` and `ClassificationComment` |

## How a value gets into this file

1. Deploy the rule and let it run for a measurement window (30 days is the reference window).
2. Run the query for the field in `tests/validation/performance-metrics.md`.
3. Record the observation, its window and its workspace in that file — **not** in this
   generated table, which is about the rules, not about one tenant's data.
4. If the number changes how the rule should be tuned, that is a tuning-log entry, and the
   rule's `metadata.tuningGuidance` is where the reasoning goes.
