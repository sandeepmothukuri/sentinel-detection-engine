# Validation Ledger

Auto-generated by `scripts/generate_atomics_ledger.py` from `tests/validation/atomics.yaml`. Do not edit by hand.

- **Rules covered:** 28 (18 scheduled analytics rules, 10 hunting queries)
- **Ledger schema:** `tests/validation/validation-schema.yaml` v1.0 · last reviewed 2026-09-11
- **Atomic source:** `github.com/redcanaryco/atomic-red-team` (vendored snapshot: `scripts/atomic_data.json`)
- **Status:** 28 STATIC VALIDATION

> **No detection in this repository has been executed against a live tenant by the author.** Every rule below is at `STATIC VALIDATION`: it passes schema, KQL lint, ATT&CK coherence, telemetry-to-connector parity and metadata checks in CI, and nothing more. The `Trigger` column says how the rule would be exercised (`SIMULATED` or `VALIDATED IN LIVE TENANT`), and the procedure for doing so is in `tests/validation/live-validation-guide.md`.

## Ledger

| Rule | Kind | Techniques | Method | Atomic tests | Evidence | Status |
|---|---|---|---|---|---|---|
| `EntraID_ImpossibleTravel`<br>Entra ID - Impossible Travel Between Sign-Ins | detection | `T1078.004`, `T1539` | Manual procedure<br><br>Sign in to a test account from one VPN egress, then within five minutes sign in again from an egress in a different country. Two interactive sign-ins more than 500 km apart inside the travel window is the trigger. Record both sign-in times and both egress locations. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>No upstream atomic produces Entra ID sign-in telemetry, so this rule cannot be validated with Atomic Red Team. T1078.004's atomics create cloud identities or runbooks, and T1539's atomics steal browser cookies on an endpoint; neither produces the two-sign-in sequence this rule measures. | STATIC VALIDATION |
| `EntraID_MFAFatigue`<br>Entra ID - MFA Fatigue Followed by Success | detection | `T1621`, `T1110.003` | Manual procedure<br><br>With a test account, drive five or more failed MFA prompts from a single IP address and then one success from the same address inside 30 minutes. An adversary-in-the-middle toolkit in a lab reaches this state fastest; a script issuing sign-ins with a wrong TOTP is the manual equivalent. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>T1621 has no upstream atomics, and T1110.003's password-spray atomics produce failed password sign-ins rather than MFA challenges, which are a different log shape. This rule needs a deliberate sequence. | STATIC VALIDATION |
| `EntraID_LegacyAuthSuccess`<br>Entra ID - Successful Legacy Auth Sign-In | detection | `T1110`, `T1078.004` | Manual procedure<br><br>Authenticate a test mailbox over SMTP AUTH, for example `Send-MailMessage -SmtpServer smtp.office365.com -Port 587 -UseSsl` using an account for which basic auth is still permitted. The resulting ClientAppUsed value is "Other clients; SMTP". | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>T1110 has no upstream atomics. The technique this rule really exercises is legacy protocol use rather than password guessing, and it requires a tenant where legacy authentication is not yet blocked — which is the scenario the rule exists to detect. | STATIC VALIDATION |
| `EntraID_ServicePrincipalCredAdd`<br>Entra ID - Service Principal Credential Added | detection | `T1098.001` | Atomic Red Team | **Trigger**<br>`T1098.001-1` — Azure AD Application Hijacking - Service Principal<br>`T1098.001-2` — Azure AD Application Hijacking - App Registration | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>Both atomics need the AzureAD PowerShell module and a privileged test account. Confirm the resulting AuditLogs row carries TargetResources[].modifiedProperties with the credential keys; if the tenant redacts them, the rule will not have enough to alert on and that is a finding about the rule, not the atomic. | STATIC VALIDATION |
| `EntraID_PrivilegedRoleAssignment`<br>Entra ID - Privileged Role Granted to User or Service Principal | detection | `T1098.003` | Atomic Red Team | **Trigger**<br>`T1098.003-1` — Azure AD - Add Company Administrator Role to a user<br><br>**Partial**<br>`T1098.003-2` — Simulate - Post BEC persistence via user password reset followed by user added to company administrator role _(partial: Password reset followed by a role addition. The reset produces an unrelated audit event, so only the second half of this test exercises the rule.)_ | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>Add-MsolRoleMember is deprecated but still writes an "Add member to role" audit event, which is what the rule matches. Verify on your tenant that Role.DisplayName is populated before relying on the mapping. | STATIC VALIDATION |
| `M365_InboxRuleExfil`<br>M365 - Suspicious Inbox Rule (Forward / Delete) | detection | `T1114.003`, `T1564.008` | Atomic Red Team | **Trigger**<br>`T1114.003-1` — Office365 - Email Forwarding<br>`T1564.008-1` — New-Inbox Rule to Hide E-mail in M365 | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>Requires Connect-ExchangeOnline against a test tenant. Confirm the OfficeActivity row for the New-InboxRule operation is present before concluding the rule is broken: the Outlook desktop client produces UpdateInboxRules events with a different shape. | STATIC VALIDATION |
| `M365_MassSharePointDownload`<br>M365 - Mass SharePoint / OneDrive Download | detection | `T1213.002`, `T1567.002` | Manual procedure<br><br>Using PnP PowerShell against a test site collection, download 200 or more files inside one hour with `Get-PnPFile` in a loop. The account must have baseline history from the preceding 14 days, otherwise the rule takes the no-baseline branch and the test proves less than intended. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>T1213.002 has no upstream atomics. T1567.002's atomics exfiltrate data to cloud storage with rclone, which is a different data source (endpoint network telemetry, not OfficeActivity) and does not exercise this rule. | STATIC VALIDATION |
| `M365_OAuthConsentSuspiciousApp`<br>M365 - OAuth Consent to High-Risk Scopes | detection | `T1528`, `T1098.001` | Manual procedure<br><br>Register a test application requesting Mail.Read and Files.ReadWrite.All, then grant user consent to it from a test account (myapps → Permissions). Confirm the "Consent to application" audit event carries the ConsentAction.Permissions modified property the rule parses. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>Upstream T1528 atomics cover Azure Functions identity-token theft, not OAuth consent grants, so they are deliberately not cited: citing them would imply a validation that cannot occur. This is the closest the repository gets to a reusable consent-grant test, and it is a manual procedure. | STATIC VALIDATION |
| `MDE_LOLBin_Rundll32_Network`<br>MDE - rundll32 With External Network Connection | detection | `T1218.011`, `T1071.001` | Atomic Red Team | **Trigger**<br>`T1218.011-1` — Rundll32 execute JavaScript Remote Payload With GetObject | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>The atomic's destination port must fall inside the rule's suspicious_ports set (80/443/8080/8443/53); HTTPS is 443, so it does. Run on an isolated test host with controlled internet egress. If egress is blocked, the atomic fails before the network event is generated — a blocked test is not a negative result, it is an untested rule. | STATIC VALIDATION |
| `MDE_MSHTA_RemoteScript`<br>MDE - mshta Executing Remote Script | detection | `T1218.005` | Atomic Red Team | **Trigger**<br>`T1218.005-3` — Mshta Executes Remote HTML Application (HTA)<br>`T1218.005-6` — Invoke HTML Application - Direct download from URI | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>The rule matches on the command line, so execution does not need to succeed for the alert to fire; blocking egress still validates the detection. | STATIC VALIDATION |
| `MDE_PowerShell_EncodedCommand`<br>MDE - PowerShell EncodedCommand With Long Payload | detection | `T1059.001`, `T1027` | Atomic Red Team | **Trigger**<br>`T1059.001-15` — ATHPowerShellCommandLineParameter -EncodedCommand parameter variations<br>`T1059.001-16` — ATHPowerShellCommandLineParameter -EncodedCommand parameter variations with encoded arguments<br><br>**Partial**<br>`T1027-2` — Execute base64-encoded PowerShell _(partial: Execute base64-encoded PowerShell: uses -EncodedCommand, but with a short payload the command line may fall under the rule's length floor.)_ | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>The rule requires a Base64 payload above a length floor. Check the generated command line length before declaring the rule silent; a short encoded payload is a negative test of the floor, not a failure of the detection. | STATIC VALIDATION |
| `MDE_ShadowCopyDeletion`<br>MDE - Shadow Copy / Recovery Inhibited | detection | `T1490` | Atomic Red Team | **Trigger**<br>`T1490-1` — Windows - Delete Volume Shadow Copies<br>`T1490-2` — Windows - Delete Volume Shadow Copies via WMI<br>`T1490-3` — Windows - wbadmin Delete Windows Backup Catalog<br>`T1490-4` — Windows - Disable Windows Recovery Console Repair<br>`T1490-13` — Windows - Delete Volume Shadow Copies via Diskshadow | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>Destructive to the test host's recovery state by design. Run on a disposable virtual machine and snapshot it first. | STATIC VALIDATION |
| `MDE_Ransomware_MassFileRename`<br>MDE - Ransomware-Style Mass File Rename Per Device | detection | `T1486` | Manual procedure (atomically incomplete)<br><br>Create 300 or more small dummy files in a scratch directory on an isolated test host, then bulk-rename them to a different extension, for example `Get-ChildItem *.txt | Rename-Item -NewName {$_.BaseName + '.locked'}`. Confirm the events arrive as ActionType == "FileRenamed" with PreviousFileName populated. This changes extensions without encrypting, which is exactly the predicate the rule keys on. | **Partial**<br>`T1486-10` — Akira Ransomware drop Files with .akira Extension and Ransomnote _(partial: Akira Ransomware drop Files with .akira Extension and Ransomnote: writes 100 new files and a ransom note, and never renames anything, so it does not produce the FileRenamed events this rule counts. Useful for confirming that file telemetry reaches the workspace.)_ | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>Recorded as partial rather than trigger deliberately. The rule measures rename volume across a device inside a window; no upstream atomic produces that shape, and claiming otherwise would be a fabricated validation. | STATIC VALIDATION |
| `MDE_WinRM_RemoteExecution`<br>MDE - WinRM Remote Command Execution | detection | `T1021.006` | Atomic Red Team | **Trigger**<br>`T1021.006-2` — Remote Code Execution with PS Credentials Using Invoke-Command<br>`T1021.006-3` — WinRM Access with Evil-WinRM<br><br>**Precondition**<br>`T1021.006-1` — Enable Windows Remote Management | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>Telemetry appears on the target device. Onboard both the source and the target to Defender for Endpoint, or the test will look silent. | STATIC VALIDATION |
| `MDE_PsExec_ServiceExecution`<br>MDE - PsExec-Style Service Execution | detection | `T1569.002`, `T1021.002` | Atomic Red Team | **Trigger**<br>`T1569.002-1` — Execute a Command as a Service<br>`T1569.002-2` — Use PsExec to execute a command on a remote host<br>`T1569.002-3` — psexec.py (Impacket)<br>`T1021.002-3` — Copy and Execute File with PsExec | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>Requires a second Windows host as the target with SMB reachable. Atomic 3 needs Python and Impacket on the test host. | STATIC VALIDATION |
| `Azure_NSG_OpenToInternet`<br>Azure - NSG Rule Opens Port to 0.0.0.0/0 | detection | `T1190`, `T1686.001` | Manual procedure<br><br>`az network nsg rule create -g <rg> --nsg-name <nsg> -n test-open3389 --priority 1000 --source-address-prefixes '*' --destination-port-ranges 3389 --access Allow --protocol Tcp --direction Inbound` then delete the rule afterwards. The subscription's activity log must be connected to the workspace or no telemetry is produced. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>T1190 and T1686.001 have no upstream atomics. If the NSG lives in a subscription that is not covered by the Azure Activity connector, the test proves nothing about the rule. | STATIC VALIDATION |
| `Azure_KeyVault_SecretAccessSpike`<br>Azure - Key Vault Secret Access Spike Per Identity | detection | `T1555.005`, `T1087.004` | Manual procedure<br><br>Give a test service principal read access to a vault, seed 30 or more secrets, then loop `az keyvault secret show --id <uri>` across all of them inside one hour. Diagnostic settings must be enabled on the vault with audit events before this telemetry exists at all. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>T1555.005 has no upstream atomics. The technique is a stand-in for credential access from a vault rather than password-manager theft, and the rule is validated with a vault-side procedure rather than an endpoint test. | STATIC VALIDATION |
| `Azure_KeyVault_AccessControlChange`<br>Azure - Key Vault Access Policy or Network ACL Modified | detection | `T1098`, `T1686.001` | Manual procedure<br><br>`az keyvault set-policy --name <vault> --upn <test-account> --secret-permissions get list` followed by `az keyvault update --name <vault> --default-action Allow`. Revert both afterwards; the second command is the higher-severity variant and is the one that opens the vault to the network. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>T1098's Azure atomics add users or service principals to Azure RBAC roles, which is a different control plane from a Key Vault access policy, and T1686.001 has no upstream atomics. Neither is cited, because neither would produce the vault diagnostic event this rule reads. | STATIC VALIDATION |
| `HUN_FirstSeenASN_PerUser`<br>HUN - First-Seen ASN per User (30-day baseline) | hunt | `T1078.004` | Manual procedure<br><br>Sign a test account in through a VPN provider whose autonomous system the account has not used in the preceding 30 days, then confirm the ASN appears as new for that user. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>No upstream atomic produces Entra ID sign-in telemetry: T1078.004's atomics create cloud service accounts and runbooks, which are control-plane events and never appear in the interactive sign-in log this hunt reads. | STATIC VALIDATION |
| `HUN_SignInFromDatacenterASN`<br>HUN - Sign-In From Hosting / Datacenter ASN | hunt | `T1078.004`, `T1090.003` | Manual procedure<br><br>Sign a test account in from a VPS in one of the autonomous systems listed in the hunt, for example OVH 16276, DigitalOcean 14061 or Hetzner 24940. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>T1090.003's atomics exercise Tor and Psiphon on an endpoint, which produces endpoint network telemetry rather than an Entra ID sign-in, so they do not validate this hunt. | STATIC VALIDATION |
| `HUN_AnomalousMailboxForwarding`<br>HUN - Mailbox Forwarding to External Domain | hunt | `T1114.003` | Manual procedure<br><br>`Set-Mailbox -Identity <test-mailbox> -ForwardingSmtpAddress "smtp:external@example.org"` against a test mailbox, then `Set-Mailbox -Identity <test-mailbox> -ForwardingSmtpAddress $null` to revert. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>The only upstream T1114.003 atomic creates an inbox rule with New-InboxRule rather than a mailbox-level forwarding address, so it does not validate this hunt. It is cited against M365_InboxRuleExfil, which is the rule it actually exercises. This is the distinction between mail-forwarding and inbox-rule persistence, and conflating the two is a common detection-mapping error. | STATIC VALIDATION |
| `HUN_GuestUserInvitedToPrivilegedGroup`<br>HUN - Guest User Added To Privileged Role / Group | hunt | `T1098.003`, `T1136.003` | Manual procedure (atomically incomplete)<br><br>Invite an external test account, accept the invitation, then add the guest to Application Administrator and confirm the audit event's target user principal name contains #EXT#. | **Precondition**<br>`T1136.003-2` — Azure AD - Create a new user | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>No upstream atomic creates an external guest identity. The hunt's guest predicate must be validated with the manual procedure; the atomic above only exercises the role-assignment half of the query. | STATIC VALIDATION |
| `HUN_NewScheduledTask`<br>HUN - New Scheduled Task With Script Action | hunt | `T1053.005` | Atomic Red Team | **Trigger**<br>`T1053.005-2` — Scheduled task Local<br>`T1053.005-4` — Powershell Cmdlet Scheduled Task | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>The hunt also requires a script interpreter or a living-off-the-land binary in the task action. Confirm the atomic's action matches that filter before concluding the hunt is broken. | STATIC VALIDATION |
| `HUN_OfficeChildProcess`<br>HUN - Office Apps Spawning Script Interpreters | hunt | `T1566.001`, `T1059` | Atomic Red Team | **Trigger**<br>`T1566.001-2` — Word spawned a command shell and used an IP address in the command line<br><br>**Precondition**<br>`T1566.001-1` — Download Macro-Enabled Phishing Attachment | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>Requires Microsoft Office on the test host. The delivery atomic alone will not fire the hunt; only the execution step produces the event. | STATIC VALIDATION |
| `HUN_RareProcessPerDevice`<br>HUN - Rare Process Per Device (org-wide rarity) | hunt | `T1059`, `T1027` | Manual procedure<br><br>Copy a benign, uniquely named executable (a freshly compiled hello-world, or any signed tool with a hash you have never run in the estate) to a single test device and run it once, then confirm it appears with a device count of one. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>The hunt measures estate-relative hash rarity. That is a property of an environment, not of a binary, so no fixed atomic can reproduce it. Citing an obfuscation atomic here would imply a test that cannot fire the query. | STATIC VALIDATION |
| `HUN_UnsignedBinaryFromTemp`<br>HUN - Unsigned Binary Execution From Temp/AppData | hunt | `T1204.002`, `T1027` | Manual procedure<br><br>Build or download an unsigned portable executable, drop it in the user's temporary folder and run it. Confirm AdditionalFields.IsSigned is populated on the device before treating a null result as a detection failure. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>T1204.002's closest atomics drop a .lnk (test 10) or a .bat (test 13) rather than an unsigned executable, so neither satisfies the hunt's extension filter and neither is cited. | STATIC VALIDATION |
| `HUN_DNSRequestsToFreeDynamicDomains`<br>HUN - DNS Queries to Free / Dynamic / Tunneling TLDs | hunt | `T1071.004`, `T1568.002` | Manual procedure<br><br>From a test device, resolve a hostname under one of the listed dynamic-DNS providers, for example `Resolve-DnsName testhost.duckdns.org`. DNS-aware network inspection must be in place for the query to be visible in device network telemetry. | — | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>T1071.004's atomics exercise DNS query volume, beaconing, and long-domain patterns against attacker-controlled domains; none resolves a free dynamic-DNS provider hostname, which is what this hunt filters on. T1568.002 is a Python DGA simulation with no registered-domain component. | STATIC VALIDATION |
| `HUN_NewSSHConnectionFromInternal`<br>HUN - Lateral SSH/RDP From Workstation | hunt | `T1021.001`, `T1021.004` | Manual procedure (atomically incomplete)<br><br>For the SSH branch: from a test workstation, connect with `ssh <user>@<lab-host>` to three distinct internal hosts inside one hour and confirm the connections are classified as private, non-public addresses. | **Partial**<br>`T1021.001-1` — RDP to Remote Host _(partial: RDP to Remote Host: produces RDP network telemetry from a workstation, covering the 3389 branch. A single execution will not clear the hunt's fan-out floor; run it against three distinct internal targets inside the window.)_ | CI: schema, KQL lint, ATT&CK coherence pass (scripts/ci_validate.py)<br><br>T1021.004's two atomics enable SSH on ESXi hosts; they do not create a client SSH connection, so they cannot validate this hunt and are not cited. The fan-out requirement (three or more distinct internal targets) is a property of the hunt, not of any single atomic. | STATIC VALIDATION |

## Techniques with no upstream atomics

These techniques are referenced by rules in this repository, and Atomic Red Team publishes no test for them. A rule mapped to one of these techniques can only be validated with a written manual procedure: `T1087.004`, `T1110`, `T1190`, `T1213.002`, `T1555.005`, `T1621`, `T1686.001`.

## Reading a status

| Status | Meaning |
|---|---|
| `STATIC VALIDATION` | Passes CI checks. Nothing has been executed. |
| `SIMULATED` | Exercised against deliberately produced telemetry outside production, with a dated evidence record. |
| `VALIDATED IN LIVE TENANT` | Fired on real telemetry in an authorised tenant, with the incident reviewed by a human and a dated evidence record. |
| `NOT YET VALIDATED` | In the repository, not yet statically validated or executed. |

Status definitions and evidence requirements live in `tests/validation/validation-schema.yaml`. The procedure for moving a rule from `STATIC VALIDATION` to `SIMULATED` or `VALIDATED IN LIVE TENANT` is in `tests/validation/live-validation-guide.md`, and the record format is `tests/validation/evidence-template.md`.
